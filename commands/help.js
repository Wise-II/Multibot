const { dir } = require("console");
const { MessageEmbed } = require("discord.js");
const { readdirSync } = require("fs");

module.exports = {
  name: 'help',
  description: 'informs the user of the dynahelp',
  usage: 'm?help'
}
